<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>モンスター検索</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>トリコ！！モンスター検索</h1>
    <form action="/search" method="get">
        <label for="query">モンスター検索！</label>
        <!-- テキスト入力フィールドを追加 -->
        <input type="text" id="query" name="q" placeholder="モンスター名を入力してください">
        <!-- 検索ボタンを追加 -->
        <button type="submit">検索</button>
    </form>
    <h2>捕獲レベル検索！</h2>
    <!-- 検索結果を表示するための領域 -->
    <ul>
        <% if (typeof results !== 'undefined' && results && results.length > 0) { %>
            <% for (let i = 0; i < results.length; i++) { %>
                <li><%= results[i] %></li>
            <% } %>
        <% } else { %>
            <p></p>
        <% } %>
    </ul>       
</body>
</html>

